server:
  port: 9000
spring:
  application:
    name: zephyr-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
    nacos:
      discovery:
        server-addr:  http://localhost:8848
